<UserControl x:Class="Application.Views.ColourList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Application.Views"
             xmlns:viewModels="clr-namespace:RealtimeDrawingApplication.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="250">
    <Grid Width="250">
        <ListBox Height="auto" Width="250"
                 ItemsSource="{Binding ColourListItems}">
        </ListBox>
        
    </Grid>
</UserControl>

